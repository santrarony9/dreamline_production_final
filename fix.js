const fs = require('fs'); const path = require('path'); function walk(dir) { let results = []; const list = fs.readdirSync(dir); list.forEach(file => { file = path.join(dir, file); const stat = fs.statSync(file); if (stat && stat.isDirectory()) { results = results.concat(walk(file)); } else { if(file.endsWith('.js')) results.push(file); } }); return results; } const files = walk('src/app/admin'); files.forEach(f => { let code = fs.readFileSync(f, 'utf8'); if (code.includes('bg-[#c5a059] text-black px-12')) { fs.writeFileSync(f, code.replace(/bg-\\[#c5a059\\] text-black px-12 py-4 rounded-full font-black uppercase\\s+tracking-widest hover:bg-white transition-all transform active:scale-95 disabled:opacity-50/g, 'bg-transparent text-[#c5a059] border border-[#c5a059]/30 hover:bg-[#c5a059]/10 px-6 py-2 rounded text-[10px] font-black uppercase tracking-widest transition-all disabled:opacity-50')); console.log('Updated ' + f); } });
