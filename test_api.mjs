import axios from 'axios';

async function testPost() {
    try {
        // Need to bypass authentication or provide a session
        // Let's just directly update the DB instead to see if Mongoose allows it
    } catch(err) {
        console.error(err);
    }
}
testPost();
